alert('dzeg');
// $("#follow-button").fadeOut();
// $("#unfollow-button").fadeIn();
// $(".follow-unfollow").html("<%= link_to 'Destroy', Follow.all(:conditions => ['user_id = ? AND followee_id = ?', current_user.id, @followee.id]), :class => 'btn btn-default btn-lg', :id => 'unfollow-button', remote: true, method: :delete %>")
$(".follow-unfollow").remove()