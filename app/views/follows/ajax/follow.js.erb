$("#follow-unfollow").html("<%= j link_to 'Destroy', Follow.all(:conditions => ['user_id = ? AND followee_id = ?', current_user.id, @followee.id]), :class => 'btn btn-default btn-lg', :id => 'unfollow-button', remote: true, method: :delete %>")