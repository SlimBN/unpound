<h1>Listing reads</h1>

<table>
  <tr>
    <th>User</th>
    <th>Article</th>
    <th>Journal</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reads.each do |read| %>
  <tr>
    <td><%= read.user_id %></td>
    <td><%= read.article_id %></td>
    <td><%= read.journal_id %></td>
    <td><%= link_to 'Show', read %></td>
    <td><%= link_to 'Edit', edit_read_path(read) %></td>
    <td><%= link_to 'Destroy', read, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Read', new_read_path %>
