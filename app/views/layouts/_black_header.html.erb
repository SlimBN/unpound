<div id="fb-root"></div>
<script>(function(d, s, id) {
  var js, fjs = d.getElementsByTagName(s)[0];
  if (d.getElementById(id)) return;
  js = d.createElement(s); js.id = id;
  js.src = "//connect.facebook.net/fr_FR/all.js#xfbml=1&appId=153439174806312";
  fjs.parentNode.insertBefore(js, fjs);
}(document, 'script', 'facebook-jssdk'));</script>

<div id="black_header">
	<div class="white-logo">
		<%= link_to (image_tag "newspaper.png"), root_path() %>
	</div>
	<% if user_signed_in? %>
		<div class="user">
			<%= link_to (image_tag current_user.avatar_url().to_s), current_user %>
			<%= link_to current_user.username, current_user %>
		</div>
	<% end %>
	
	<% if user_signed_in? %>
	<span>
		<%= link_to "Se Déconnecter", logout_path() %>
	</span>
	<% else %>
	<span>
		<%= link_to "S'enregistrer'", register_path() %>
	</span>
	<span>
		<%= link_to "Se Connecter", login_path() %>
	</span>
	<% end %>

	<span>
		login
	</span>
	<span>
		login
	</span>
	<span>
		login
	</span>

</div>